# Required: URL of the deployed API backend (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.azurewebsites.net

# Optional: Stripe publishable key (for client-side Stripe.js, if needed later)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx

# Set to "true" to use mock data instead of calling the API (dev only)
# NEXT_PUBLIC_USE_MOCKS=true
